apiVersion: v1
kind: Service
metadata:
  name: airflow-api-server
  namespace: airflow
spec:
  type: NodePort
  selector:
    app: airflow-api-server
    component: apiserver
  ports:
    - port: 8080
      targetPort: 8080
