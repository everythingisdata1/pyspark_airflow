apiVersion: v1
kind: Service
metadata:
  name: airflow-scheduler
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: airflow-scheduler
  ports:
    - name: log
      port: 8793
      targetPort: 8793
